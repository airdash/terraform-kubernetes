apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-csi-delete-example
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi-delete
